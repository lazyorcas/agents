# Storybook

Rules:
- Stories should demonstrate:
  - Default usage
  - Key variants / sizes
  - Disabled / loading / empty states
  - Edge cases (long labels, narrow containers)
- Use meaningful mock data.
- Keep stories deterministic (no real network, no time-dependent output).